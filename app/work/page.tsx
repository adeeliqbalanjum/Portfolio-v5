import Link from'next/link';import{projects}from'@/data/projects';export const metadata={title:'Work — Muhammad Adeel Iqbal'};export default function Work(){return <section className="wrap section" style={{paddingTop:150}}><h1 className="headline">Selected WordPress work and case studies</h1><p className="lead" style={{marginLeft:0}}>Healthcare, travel, booking, corporate, law, education, local services and consulting websites.</p><div className="grid projects" style={{marginTop:34}}>{projects.map(p=><article className="card" key={p.slug}><h3>{p.title}</h3><p className="muted">{p.summary}</p><div className="chips">{p.skills.split(', ').map(s=><span className="chip" key={s}>{s}</span>)}</div><p><Link className="btn" href={`/work/${p.slug}`}>Case Study</Link> <a className="btn alt" href={p.url}>Live</a></p></article>)}</div></section>}
