import Link from'next/link';import{notFound}from'next/navigation';import{getProject,projects}from'@/data/projects';export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}export function generateMetadata({params}:{params:{slug:string}}){const p=getProject(params.slug);return{title:p?`${p.title} Case Study — WordPress Website`:'Project Case Study',description:p?.summary}}export default function Case({params}:{params:{slug:string}}){const p=getProject(params.slug);if(!p)notFound();return <><section className="wrap section" style={{paddingTop:150}}><span className="eyebrow">{p.cat}</span><h1 className="headline">{p.title} Case Study</h1><p className="lead" style={{marginLeft:0}}>{p.summary}</p><div className="actions" style={{justifyContent:'flex-start'}}><a className="btn" href={p.url}>View Live Website</a><Link className="btn alt" href="/work">All Projects</Link></div></section><section className="wrap section grid" style={{gridTemplateColumns:'repeat(2,1fr)',paddingTop:0}}>{['Challenge','My role','Solution','Outcome'].map((h,i)=><div className="card" key={h}><h3>{h}</h3><p className="muted">{i==0?'The website needed stronger clarity, credibility, responsive structure and a smoother inquiry path.':i==1?'WordPress development, Elementor layout building, responsive optimization, forms, custom styling, QA and launch support.':i==2?'The site was structured around clear sections, service hierarchy, conversion-focused CTAs, trust elements and mobile-first layouts.':'Cleaner presentation, stronger mobile experience, easier navigation and a more professional online presence.'}</p></div>)}</section><section className="wrap section" style={{paddingTop:0}}><h2 className="title">Tech stack and features</h2><div className="chips">{p.skills.split(', ').concat(['Responsive Design','SEO-ready Structure','CTA Flow','Mobile Optimization']).map(s=><span className="chip" key={s}>{s}</span>)}</div></section></>}
